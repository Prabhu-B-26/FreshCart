
"use client";

// This component is currently disabled in the frontend-only version.
// It can be re-enabled if AI/backend functionality is restored.
export default function RecommendedProducts() {
  return null;
}
